# Dicionario métodos úteis

d = {"a": 1, "b": 2}

d.keys() # retorna as chaves
d.values() # retorna os valores
d.items() # retorna os pares (chave, valor)
#d.clear() # Limpa o dicionário

print(d["b"])